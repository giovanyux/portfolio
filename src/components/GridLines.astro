---
interface Props {
  lines?: boolean;
  glow?: boolean;
}
const { lines = true, glow = false } = Astro.props;
---
{lines && <div class="grid-lines" aria-hidden="true"></div>}
{glow && <div class="hero-glow" aria-hidden="true"></div>}
