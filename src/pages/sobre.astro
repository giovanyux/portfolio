---
import BaseLayout from '@/layouts/BaseLayout.astro';
import GridLines from '@/components/GridLines.astro';

const experience = [
  {
    company: 'CARDÁPIO WEB',
    role: 'Líder Designer de Produto',
    years: 'AGO 2025 - JUN 2026',
    desc: 'Liderança de toda a frente de design em plataforma SaaS food service. Redesign B2B (+13k clientes pagantes), melhorias B2C (+1M usuários) e lançamento do totem de auto-atendimento.',
  },
  {
    company: 'GUPY',
    role: 'Designer de Produto Sr (Designer III)',
    years: 'MAI 2024 - JAN 2025',
    desc: 'Redesign do sistema de gerenciamento de vagas: +40% na taxa de aplicações e -30% no tempo de preenchimento. Lançamento de 3 novas funcionalidades estratégicas.',
  },
  {
    company: 'CLIQX',
    role: 'Designer de Produto Sr',
    years: 'MAR 2023 - MAR 2024',
    desc: 'Design Ops e product design em 4 squads. Padronização de documentação com redução de 50% no onboarding. Redesenho de fluxos de chatbot e auto-atendimento.',
  },
  {
    company: 'SUPERLOGICA',
    role: 'Designer de Produto Sr',
    years: 'JUN 2022 - FEV 2023',
    desc: 'Produtos financeiros que atingiram 120% das OKRs trimestrais. Pesquisa com usuários e parceria com growth para +25% de conversão.',
  },
  {
    company: 'GRUPO SALTA',
    role: 'Designer de Produto Sr',
    years: 'AGO 2021 - MAI 2022',
    desc: 'Liderança em discovery para 3 novos produtos digitais. Facilitação de workshops de Design Thinking. +70% dos OKRs atingidos.',
  },
  {
    company: 'AGENDA EDU',
    role: 'Designer de Produto Pleno',
    years: 'JAN 2021 - AGO 2021',
    desc: 'Design system unificado web e app. Redesenho de fluxos críticos baseado em analytics. Sistema de gestão escolar e aplicativo de pagamentos.',
  },
  {
    company: 'INTMED',
    role: 'Designer de Produto Jr',
    years: 'NOV 2017 - DEZ 2020',
    desc: 'Primeiro designer da empresa. 9 projetos entregues para a Hapvida (emergência, clínica, batimentos fetais). Fundação do time de design.',
  },
];

const focusAreas = [
  {
    num: '01',
    title: 'Product Design',
    desc: 'Visão end-to-end de produto. Do entendimento do problema e discovery até a entrega e iteração.',
  },
  {
    num: '02',
    title: 'Design Systems',
    desc: 'Arquiteturas token-first, governança de componentes, Storybook e alinhamento design/código.',
  },
  {
    num: '03',
    title: 'Workflow com IA',
    desc: 'Claude, Cursor e prompts customizados no dia a dia: da síntese de pesquisa à prototipagem.',
  },
];

const tools = ['FIGMA', 'FIGMA MCP', 'CLAUDE', 'CLAUDE CODE', 'CURSOR', 'DOVETAIL', 'MAZE', 'HOTJAR', 'MIXPANEL', 'SEMRUSH', 'STORYBOOK', 'SHADCN/UI', 'ASTRO', 'TAILWIND', 'NOTION', 'GITHUB'];
---

<BaseLayout
  title="Sobre · Giovany Jr"
  description="Lead product designer com 9 anos. Baseado em Fortaleza, Brasil. Aberto a posições sênior/lead, remote-first."
>

  <!-- ── HERO ──────────────────────────────────────────── -->
  <section style="position: relative; overflow: hidden; padding: 5rem 2rem 5rem; max-width: 1200px; margin: 0 auto;">
    <GridLines />
    <div style="position: relative; z-index: var(--z-base);">
      <p data-reveal="hero" style="font-family: var(--font-mono); font-size: 0.65rem; letter-spacing: 0.14em; text-transform: uppercase; color: var(--text-muted); margin-bottom: var(--space-md);">
        SOBRE
      </p>
      <h1 data-reveal="hero" style="font-family: var(--font-display); font-size: var(--text-display); font-weight: 600; letter-spacing: -0.03em; line-height: 1; margin-bottom: var(--space-md); transition-delay: 100ms;">
        Olá, sou Giovany.
      </h1>
      <p data-reveal="hero" style="font-family: var(--font-sans); font-size: 1rem; color: var(--text-muted); transition-delay: 180ms;">
        Lead product designer baseado em Fortaleza, Brasil.
      </p>
    </div>
  </section>

  <!-- ── BIO ───────────────────────────────────────────── -->
  <section style="max-width: 1200px; margin: 0 auto; padding: 0 2rem 5rem;">
    <div style="max-width: 640px;">
      {[
        'Nove anos desenhando produtos digitais. Comecei em SaaS médico como UI designer e cresci até liderar design de plataformas em escala.',
        'Meu trabalho mais recente foi food-tech, software operacional que faz restaurante funcionar. Interface que aguenta pressão. Cozinha barulhenta. Mãos ocupadas. Operador com 30 segundos pra decidir.',
        'Nos últimos dois anos integrei IA em todas as fases do meu trabalho. Claude pra síntese de pesquisa e escrita. Cursor pra prototipagem com código real. Claude Code pra construir projetos inteiros, como este portfólio. Não é atalho. É multiplicador de capacidade.',
        'Procuro posição senior ou lead em product design, remote, em produto com complexidade operacional de verdade.',
      ].map((para) => (
        <p style="font-family: var(--font-sans); font-size: 1rem; line-height: 1.7; color: var(--text-muted); margin-bottom: var(--space-md);">
          {para}
        </p>
      ))}
    </div>
  </section>

  <!-- ── EXPERIÊNCIA — tratamento tipo changelog ─────────── -->
  <section style="max-width: 1200px; margin: 0 auto; padding: 0 2rem 5rem; border-top: 1px solid var(--border);">
    <h2 style="font-family: var(--font-mono); font-size: 0.65rem; font-weight: 700; letter-spacing: 0.12em; text-transform: uppercase; color: var(--text-muted); padding: var(--space-xl) 0 var(--space-lg);">Experiência</h2>

    <div class="changelog">
      {experience.map((item, i) => (
        <div class="changelog-entry">
          <div class="changelog-meta">
            <span class="changelog-tag tabular-nums">v{experience.length - i}.0</span>
            <span class="changelog-years tabular-nums">{item.years}</span>
          </div>
          <div>
            <p style="font-family: var(--font-display); font-size: 1rem; font-weight: 600; color: var(--text); margin-bottom: 0.2rem;">{item.company}</p>
            <p style="font-family: var(--font-mono); font-size: 0.68rem; color: var(--action); letter-spacing: 0.03em; margin-bottom: 0.6rem;">{item.role}</p>
            <p style="font-family: var(--font-sans); font-size: 0.85rem; color: var(--text-muted); line-height: 1.6;">{item.desc}</p>
          </div>
        </div>
      ))}
    </div>
  </section>

  <!-- ── ÁREAS DE FOCO ──────────────────────────────────── -->
  <section style="max-width: 1200px; margin: 0 auto; padding: 0 2rem 5rem; border-top: 1px solid var(--border);">
    <h2 style="font-family: var(--font-mono); font-size: 0.65rem; font-weight: 700; letter-spacing: 0.12em; text-transform: uppercase; color: var(--text-muted); padding: var(--space-xl) 0 var(--space-lg);">Áreas de foco</h2>

    <div data-reveal="scroll" class="hairline-card" style="display: grid; grid-template-columns: repeat(auto-fit, minmax(240px, 1fr)); overflow: hidden;">
      {focusAreas.map((area, i) => (
        <div
          style={`padding: var(--space-lg); min-width: 0; ${i < focusAreas.length - 1 ? 'border-right: 1px solid var(--border);' : ''} border-bottom: 1px solid var(--border);`}
        >
          <p class="tabular-nums" style="font-family: var(--font-mono); font-size: 0.6rem; color: var(--action); font-weight: 700; letter-spacing: 0.08em; margin-bottom: var(--space-sm);">{area.num}</p>
          <h3 style="font-family: var(--font-display); font-size: 1rem; font-weight: 600; color: var(--text); margin-bottom: var(--space-xs); letter-spacing: -0.01em;">{area.title}</h3>
          <p style="font-family: var(--font-sans); font-size: 0.85rem; color: var(--text-muted); line-height: 1.6;">{area.desc}</p>
        </div>
      ))}
    </div>
  </section>

  <!-- ── FERRAMENTAS ────────────────────────────────────── -->
  <section style="max-width: 1200px; margin: 0 auto; padding: 0 2rem 6rem; border-top: 1px solid var(--border);">
    <h2 style="font-family: var(--font-mono); font-size: 0.65rem; font-weight: 700; letter-spacing: 0.12em; text-transform: uppercase; color: var(--text-muted); padding: var(--space-xl) 0 var(--space-lg);">Ferramentas</h2>
    <p style="font-family: var(--font-mono); font-size: 0.78rem; letter-spacing: 0.08em; color: var(--text-muted); line-height: 1.9;">
      {tools.join(' · ')}
    </p>
  </section>

  <style>
    .changelog-entry {
      display: grid;
      grid-template-columns: 12rem 1fr;
      gap: var(--space-lg);
      padding: var(--space-md) 0;
      border-top: 1px solid var(--border);
      align-items: baseline;
    }

    .changelog-entry:last-child { border-bottom: 1px solid var(--border); }

    .changelog-meta {
      display: flex;
      flex-direction: column;
      gap: 0.3rem;
    }

    .changelog-tag {
      font-family: var(--font-mono);
      font-size: 0.7rem;
      font-weight: 700;
      color: var(--text);
      background: var(--surface);
      border: 1px solid var(--border);
      border-radius: var(--radius-sm);
      padding: 0.15em 0.5em;
      width: fit-content;
    }

    .changelog-years {
      font-family: var(--font-mono);
      font-size: 0.62rem;
      color: var(--text-muted);
      letter-spacing: 0.04em;
    }

    @media (max-width: 640px) {
      .changelog-entry {
        grid-template-columns: 1fr;
        gap: var(--space-sm);
      }
    }
  </style>

</BaseLayout>
