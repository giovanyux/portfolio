---
import BaseLayout from './BaseLayout.astro';
import Gate from '@/components/Gate.astro';
import NextCase from '@/components/NextCase.astro';
import { BASE } from '@/consts';

interface Props {
  title: string;
  description: string;
  cover: string;
  coverVideo?: string;
  tags: string[];
  metric: string;
  metricLabel?: string;
  client: string;
  role: string;
  industry?: string;
  platform?: string;
  duration: string;
  year: number;
  next?: { title: string; slug: string; tag: string };
}

const {
  title, description, cover, coverVideo, tags, metric, metricLabel,
  client, role, industry, platform, duration, year, next,
} = Astro.props;
---

<BaseLayout title={`${title} · Giovany Jr`} description={description} ogImage={`${BASE}${cover}`} type="article">
  <div style="max-width: 900px; margin: 0 auto; padding: 0 2rem;">

    <!-- Breadcrumb -->
    <nav
      aria-label="Breadcrumb"
      data-reveal="hero"
      style="padding: 2rem 0 1.5rem; font-family: var(--font-mono); font-size: 0.65rem; letter-spacing: 0.1em; color: var(--text-muted); display: flex; align-items: center; gap: 0.5rem; text-transform: uppercase;"
    >
      <a href={`${BASE}/`} style="color: var(--text-muted); text-decoration: none;" class="underline-hover">Home</a>
      <span style="opacity: 0.4;">/</span>
      <a href={`${BASE}/cases`} style="color: var(--text-muted); text-decoration: none;" class="underline-hover">Cases</a>
      <span style="opacity: 0.4;">/</span>
      <span style="color: var(--text);">{title}</span>
    </nav>

    <!-- Tags + year -->
    <div data-reveal="hero" style="display: flex; align-items: center; gap: 0.75rem; margin-bottom: 1.5rem; flex-wrap: wrap;">
      {tags.map((tag) => (
        <span style="font-family: var(--font-mono); font-size: 0.65rem; letter-spacing: 0.12em; color: var(--text-muted); text-transform: uppercase;">
          {tag}
        </span>
      ))}
      <span style="color: var(--border);">·</span>
      <span style="font-family: var(--font-mono); font-size: 0.65rem; letter-spacing: 0.1em; color: var(--action); font-weight: 700;">{year}</span>
    </div>

    <!-- Title -->
    <h1
      data-reveal="hero"
      style="font-family: var(--font-display); font-size: var(--text-display); font-weight: 600; line-height: 1.02; letter-spacing: -0.03em; color: var(--text); margin-bottom: var(--space-md);"
    >
      {title}
    </h1>

    <!-- Description -->
    <p
      data-reveal="hero"
      style="font-family: var(--font-sans); font-size: 1.05rem; color: var(--text-muted); line-height: 1.6; margin-bottom: var(--space-lg); max-width: 56ch;"
    >
      {description}
    </p>

    <!-- Meta grid: Empresa · Papel · Indústria · Plataforma · Duração · Ano -->
    <div
      data-reveal="hero"
      class="meta-grid hairline-card"
      style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 0; overflow: hidden; margin-bottom: var(--space-xl);"
    >
      {[
        { label: 'EMPRESA', value: client },
        { label: 'PAPEL', value: role },
        { label: 'INDÚSTRIA', value: industry ?? 'N/D' },
        { label: 'PLATAFORMA', value: platform ?? 'N/D' },
        { label: 'DURAÇÃO', value: duration },
        { label: 'ANO', value: String(year) },
      ].map(({ label, value }, i) => (
        <div style={`padding: var(--space-sm) var(--space-md); border-right: ${i % 3 < 2 ? '1px solid var(--border)' : 'none'}; border-top: ${i > 2 ? '1px solid var(--border)' : 'none'};`}>
          <p style="font-family: var(--font-mono); font-size: 0.55rem; letter-spacing: 0.12em; color: var(--text-muted); text-transform: uppercase; margin-bottom: 0.3rem;">{label}</p>
          <p class="tabular-nums" style="font-family: var(--font-mono); font-size: 0.78rem; font-weight: 700; color: var(--text); line-height: 1.3;">{value}</p>
        </div>
      ))}
    </div>

    <style>
      @media (max-width: 639px) {
        .meta-grid { grid-template-columns: repeat(2, 1fr) !important; }
      }
    </style>

    <!-- Hero image / video -->
    <div
      data-reveal="scroll"
      data-reveal-artifact
      class="img-placeholder"
      style="width: 100%; aspect-ratio: 16/9; margin-bottom: 0.5rem; border-radius: var(--radius-md);"
    >
      {coverVideo ? (
        <video
          autoplay
          loop
          muted
          playsinline
          preload="metadata"
          poster={BASE + cover}
          aria-label={`Demo animada do redesign: ${title}`}
          class="cover-media"
        >
          <source src={BASE + coverVideo} type="video/mp4" />
          <img src={BASE + cover} alt={`${title}: imagem de capa`} class="cover-media" />
        </video>
      ) : (
        <img
          src={BASE + cover}
          alt={`${title}: imagem de capa`}
          class="cover-media"
          onerror="this.style.display='none';"
        />
      )}
      <span>{title}: screenshot de capa</span>
    </div>
    <p style="font-family: var(--font-mono); font-size: 0.6rem; color: var(--text-muted); letter-spacing: 0.06em; margin-bottom: var(--space-xl); text-align: center;">
      Fig. 01 · {title}
    </p>

    <!-- Resultado — card de superfície com acento --action, era a faixa grafite -->
    <div
      data-reveal="scroll"
      data-reveal-artifact
      class="result-card"
      style="padding: var(--space-lg); margin-bottom: var(--space-xl);"
    >
      <p class="result-card-label" style="font-family: var(--font-sans); font-size: 0.65rem; font-weight: 600; letter-spacing: 0.1em; text-transform: uppercase; color: var(--text-muted); margin-bottom: var(--space-xs);">Resultado</p>
      <p class="tabular-nums" style="font-family: var(--font-display); font-size: clamp(1.5rem, 4vw, 2.25rem); font-weight: 600; color: var(--text); letter-spacing: -0.01em; line-height: 1.15;">{metric}</p>
      {metricLabel && (
        <p style="font-family: var(--font-sans); font-size: 0.85rem; color: var(--text-muted); margin-top: var(--space-xs); line-height: 1.5;">{metricLabel}</p>
      )}
    </div>

    <!-- Markdown content — cascata narrativa: cada bloco (heading, parágrafo,
         componente) revela em sequência conforme entra na tela, o atraso
         reseta a cada heading (nova seção do case). Ver tagNarrativeGroups
         em BaseLayout.astro. -->
    <article class="prose" data-reveal-narrative>
      <slot />
    </article>

    <!-- [GATE] fixo -->
    <Gate />

    <!-- [PROXIMO] -->
    {next && <NextCase title={next.title} slug={next.slug} tag={next.tag} />}
  </div>
</BaseLayout>

<style>
  .cover-media {
    width: 100%;
    height: 100%;
    object-fit: cover;
    position: absolute;
    inset: 0;
    z-index: 2;
  }
</style>

<script>
  if (window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
    document.querySelectorAll('video[autoplay]').forEach((v) => {
      (v as HTMLVideoElement).removeAttribute('autoplay');
      (v as HTMLVideoElement).pause();
    });
  }
</script>
